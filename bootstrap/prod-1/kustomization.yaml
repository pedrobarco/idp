# Bootstrap for the prod-1 cluster.
# Installs: argocd-manager SA, registry ConfigMap.
# ingress-nginx is managed via ArgoCD ApplicationSet.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../infrastructure/argocd-manager
  - ../../infrastructure/registry
