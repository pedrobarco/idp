name: CI

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    uses: http://gitea-http.gitea.svc.cluster.local:3000/admin/idp/.gitea/workflows/build-and-push.yaml@main
    runs-on: ubuntu-latest
    with:
      repository: hello-app
